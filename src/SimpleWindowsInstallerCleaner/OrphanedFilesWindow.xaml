<Window x:Class="SimpleWindowsInstallerCleaner.OrphanedFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:SimpleWindowsInstallerCleaner.Controls"
        Title="Orphaned files"
        Width="800" Height="500"
        MinWidth="500" MinHeight="320"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource WindowBackground}"
        Loaded="Window_Loaded">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="6"
                      GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- title bar -->
            <RowDefinition Height="*"/>    <!-- content -->
        </Grid.RowDefinitions>

        <!-- ── Custom title bar ────────────────────────────────────── -->
        <controls:TitleBar Grid.Row="0"
                           WindowTitle="Orphaned files"
                           ShowMinimise="False"/>

        <!-- ── Content ───────────────────────────────────────────── -->
        <Grid Grid.Row="1" Margin="{StaticResource WindowPadding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>    <!-- file list + details -->
                <RowDefinition Height="12"/>   <!-- spacer -->
                <RowDefinition Height="Auto"/> <!-- summary + close -->
            </Grid.RowDefinitions>

            <!-- ── Main area: file list (left) + details (right) ──── -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="230"/>
                </Grid.ColumnDefinitions>

                <!-- Left: file list card -->
                <Border Grid.Column="0"
                        Background="{DynamicResource CardBackground}"
                        BorderBrush="{DynamicResource CardBorder}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource CardRadius}"
                        Effect="{DynamicResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto" MaxHeight="150"/>
                        </Grid.RowDefinitions>

                        <!-- Orphaned files (actionable) -->
                        <ListBox Grid.Row="0"
                                 x:Name="ActionableList"
                                 ItemsSource="{Binding ActionableFiles}"
                                 SelectedItem="{Binding SelectedFile, Mode=TwoWay}"
                                 GotFocus="ActionableList_GotFocus"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="12,10,12,6"
                                 HorizontalContentAlignment="Stretch">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="72"/>
                                            <ColumnDefinition Width="36"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding FileName}"
                                                   Foreground="{DynamicResource TextPrimary}"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="{StaticResource BodySize}"
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding FullPath}"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding SizeDisplay}"
                                                   Foreground="{DynamicResource TextSecondary}"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="{StaticResource BodySize}"
                                                   TextAlignment="Right"/>
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding TypeLabel}"
                                                   Foreground="{DynamicResource TextTertiary}"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="{StaticResource BodySize}"
                                                   Margin="6,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Excluded header + description -->
                        <StackPanel Grid.Row="1"
                                    Visibility="{Binding HasExcludedFiles, Converter={StaticResource BoolToVis}}">
                            <Border BorderBrush="{DynamicResource BorderSubtle}"
                                    BorderThickness="0,1,0,0"
                                    Margin="12,4,12,0"
                                    Padding="0,8,0,4">
                                <TextBlock Text="Excluded by filters"
                                           Foreground="{DynamicResource TextTertiary}"
                                           FontFamily="{StaticResource AppFont}"
                                           FontSize="{StaticResource CaptionSize}"
                                           FontStyle="Italic"/>
                            </Border>
                            <TextBlock Text="These files match your exclusion filters and won't be moved or deleted."
                                       Foreground="{DynamicResource TextTertiary}"
                                       FontFamily="{StaticResource AppFont}"
                                       FontSize="{StaticResource CaptionSize}"
                                       Margin="12,0,12,4"
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                        <!-- Excluded files -->
                        <ListBox Grid.Row="2"
                                 x:Name="ExcludedList"
                                 ItemsSource="{Binding ExcludedFiles}"
                                 SelectedItem="{Binding SelectedFile, Mode=TwoWay}"
                                 GotFocus="ExcludedList_GotFocus"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="12,2,12,10"
                                 HorizontalContentAlignment="Stretch"
                                 Visibility="{Binding HasExcludedFiles, Converter={StaticResource BoolToVis}}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="72"/>
                                            <ColumnDefinition Width="36"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding FileName}"
                                                   Foreground="{DynamicResource TextTertiary}"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="{StaticResource BodySize}"
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding FullPath}"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding SizeDisplay}"
                                                   Foreground="{DynamicResource TextTertiary}"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="{StaticResource BodySize}"
                                                   TextAlignment="Right"/>
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding TypeLabel}"
                                                   Foreground="{DynamicResource TextTertiary}"
                                                   FontFamily="{StaticResource AppFont}"
                                                   FontSize="{StaticResource BodySize}"
                                                   Margin="6,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Right: details card -->
                <Border Grid.Column="2"
                        Background="{DynamicResource CardBackground}"
                        BorderBrush="{DynamicResource CardBorder}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource CardRadius}"
                        Padding="16,14"
                        Effect="{DynamicResource CardShadow}">
                    <Grid>
                        <!-- Placeholder: nothing selected -->
                        <TextBlock Text="Select a file to view details."
                                   Foreground="{DynamicResource TextTertiary}"
                                   FontFamily="{StaticResource AppFont}"
                                   FontSize="{StaticResource SmallSize}"
                                   FontStyle="Italic"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasSelection}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Placeholder: no metadata -->
                        <TextBlock Text="No metadata available."
                                   Foreground="{DynamicResource TextTertiary}"
                                   FontFamily="{StaticResource AppFont}"
                                   FontSize="{StaticResource SmallSize}"
                                   FontStyle="Italic"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center"
                                   Visibility="{Binding ShowNoMetadata, Converter={StaticResource BoolToVis}}"/>

                        <!-- Details grid -->
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Visibility="{Binding ShowDetails, Converter={StaticResource BoolToVis}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Author" Foreground="{DynamicResource TextTertiary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource CaptionSize}"/>
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding SelectedDetails.Author}" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource SmallSize}" TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Title" Foreground="{DynamicResource TextTertiary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource CaptionSize}"/>
                                <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding SelectedDetails.Title}" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource SmallSize}" TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Subject" Foreground="{DynamicResource TextTertiary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource CaptionSize}"/>
                                <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding SelectedDetails.Subject}" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource SmallSize}" TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="Signature" Foreground="{DynamicResource TextTertiary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource CaptionSize}"/>
                                <TextBlock Grid.Row="6" Grid.Column="2" Text="{Binding SelectedDetails.DigitalSignature}" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource SmallSize}" TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="8" Grid.Column="0" Text="Size" Foreground="{DynamicResource TextTertiary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource CaptionSize}"/>
                                <TextBlock Grid.Row="8" Grid.Column="2" Text="{Binding SelectedFile.SizeDisplay}" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource SmallSize}"/>

                                <TextBlock Grid.Row="10" Grid.Column="0" Text="Comment" Foreground="{DynamicResource TextTertiary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource CaptionSize}"/>
                                <TextBlock Grid.Row="10" Grid.Column="2" Text="{Binding SelectedDetails.Comments}" Foreground="{DynamicResource TextPrimary}" FontFamily="{StaticResource AppFont}" FontSize="{StaticResource SmallSize}" TextWrapping="Wrap"/>
                            </Grid>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- ── Summary + close ───────────────────────────────── -->
            <DockPanel Grid.Row="2" LastChildFill="False">
                <TextBlock DockPanel.Dock="Left"
                           Text="{Binding Summary}"
                           Foreground="{DynamicResource TextSecondary}"
                           FontFamily="{StaticResource AppFont}"
                           FontSize="{StaticResource SmallSize}"
                           VerticalAlignment="Center"/>
                <Button DockPanel.Dock="Right"
                        Style="{DynamicResource SecondaryButton}"
                        Content="Close"
                        IsCancel="True"/>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
