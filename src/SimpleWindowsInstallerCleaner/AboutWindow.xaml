<Window x:Class="SimpleWindowsInstallerCleaner.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About"
        Width="400" Height="200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Simple Windows installer cleaner"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1"
                   Text="Version 0.1.0-alpha"
                   FontSize="12"
                   Foreground="#888"
                   Margin="0,0,0,2"/>

        <TextBlock Grid.Row="2"
                   Text="Part of the No faff suite"
                   FontSize="12"
                   Foreground="#888"
                   Margin="0,0,0,2"/>

        <TextBlock Grid.Row="3"
                   FontSize="12"
                   Foreground="#888"
                   Margin="0,0,0,2">
            <Run Text="MIT licence · "/>
            <Hyperlink NavigateUri="https://github.com/no-faff/windows-installer-cleaner"
                       Click="Hyperlink_Click">github.com/no-faff/windows-installer-cleaner</Hyperlink>
        </TextBlock>

        <Button Grid.Row="5"
                Content="Close"
                IsCancel="True"
                HorizontalAlignment="Right"
                Width="80"/>
    </Grid>
</Window>
